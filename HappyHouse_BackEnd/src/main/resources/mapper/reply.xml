<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.ReplyMapper">
  
	<insert id="replyArticle" parameterType="replyDto">
		insert into boardreply (articleno, userid, subject, content, replytime)
		values (#{articleno}, #{userId}, #{subject}, #{content}, now())
	</insert>
	
	<select id="getReply" parameterType="int" resultType="replyDto">
		select replyno, articleno, userid, subject, content, replytime
		from boardreply
		where articleno = #{articleno}
	</select>
	
	<update id="updateReply" parameterType="replyDto">
		update boardreply
		set subject = #{subject}, content = #{content} 
		where articleno = #{articleno}
	</update>
	
	<delete id="deleteReply" parameterType="int">
		delete from boardreply
		where articleno = #{articleno}
	</delete>

</mapper>