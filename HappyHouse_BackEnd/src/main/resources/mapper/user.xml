<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.UserMapper">

	<select id="login" parameterType="userDto" resultType="userDto">
		select userid, username, email
		from user
		where userid = #{userid} and password = #{password}
	</select>
	
	<select id="userInfo" parameterType="String" resultType="userDto">
		select userid, password, username, email, phone
		from user
		where userid = #{userid}
	</select>
	
	<select id="idCheck" parameterType="string" resultType="int">
		select count(userid)
		from user
		where userid = #{userid}
	</select>

	<insert id="registerUser" parameterType="userDto">
		insert into user (userid, password, username, email, phone)
		values (#{userid}, #{password}, #{username}, #{email}, #{phone})
	</insert>
	
	<update id="updateUser" parameterType="userDto">
		update user
		set password = #{password}, username = #{username}, email = #{email}, phone = #{phone}
		where userid = #{userid}
	</update>
	
	<delete id="deleteUser" parameterType="String">
		delete from user
		where userid = #{userid}
	</delete>
	
</mapper>